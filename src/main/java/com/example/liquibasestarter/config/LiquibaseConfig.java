package com.example.liquibasestarter.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LiquibaseConfig {
    // This class can be used to customize Liquibase configuration if needed
    // For now, we're using the default Spring Boot auto-configuration
    // which reads from application.yml
}
